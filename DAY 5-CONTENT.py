

random -returns float values
choice -returns integer values

#FUNCTIONS
classification
1. pre-defined
2.user defined

for code resubality we use functions .lets say we want to use a particular concept multiple times in our pgminstead of writing the same code multiple times we can write it once include that inside a function and can call the function wherever we need it

#types
1. functions without argument and without return value
2. without argument with return value
3. with argument with return value
4. with argument without return value


#recursive function
a func which calls itself is called a recursive function
